enum Role {
  USER
  ADMIN
  SUPER_ADMIN
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum MediaType {
  MOVIE
  SERIES
}

enum PricingType {
  FREE
  PREMIUM
}

enum ReviewStatus {
  DRAFT
  PENDING
  PUBLISHED
  UNPUBLISHED
}

enum PurchaseType {
  BUY
  RENT
  SUBSCRIPTION
}

enum RentalDuration {
  DAYS_1
  DAYS_7
}

enum PlanType {
  MONTHLY
  YEARLY
}

enum PaymentGateway {
  STRIPE
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
  REFUNDED
}

enum SortOrder {
  LATEST
  TOP_RATED
  MOST_REVIEWED
  MOST_LIKED
}

enum SubscriptionStatus {
  ACTIVE
  INACTIVE
  CANCELLED
  PAST_DUE
}
