enum Role {
  USER
  ADMIN
  SUPER_ADMIN
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum MediaType {
  MOVIE
  SERIES
}

enum PricingType {
  FREE
  PREMIUM
}

enum ReviewStatus {
  PENDING
  PUBLISHED
  UNPUBLISHED
}

enum PurchaseType {
  BUY
  RENT
  SUBSCRIPTION
}

enum RentalDuration {
  HOURS_48
  DAYS_7
  DAYS_30
}

enum PlanType {
  MONTHLY
  YEARLY
}

enum PaymentGateway {
  STRIPE
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
  REFUNDED
}

enum SortOrder {
  LATEST
  TOP_RATED
  MOST_REVIEWED
  MOST_LIKED
}
